'''
Định Dạng & Làm Sạch
len(s): Lấy độ dài (số ký tự) của chuỗi.

s.strip(): Xóa khoảng trắng dư thừa ở đầu và cuối chuỗi.

s.lower(): Chuyển toàn bộ chuỗi thành chữ thường.

s.upper(): Chuyển toàn bộ chuỗi thành chữ hoa.

s.title(): Viết hoa chữ cái đầu tiên của mỗi từ.


s.isdigit(): Kiểm tra chuỗi có chỉ chứa chữ số không.

s.isalpha(): Kiểm tra chuỗi có chỉ chứa chữ cái không.

s.isalnum(): Kiểm tra chuỗi có chỉ chứa chữ cái và số không.

len(s)	Trả về độ dài chuỗi s
s.upper()	Chuyển chuỗi thành chữ hoa
s.lower()	Chuyển chuỗi thành chữ thường
s.title()	Viết hoa chữ cái đầu mỗi từ
s.strip()	Xóa khoảng trắng ở đầu và cuối chuỗi
s.replace(a, b)	Thay thế chuỗi a bằng b
s.split(sep)	Tách chuỗi thành danh sách theo ký tự phân tách
'sep'.join(list)	Nối các phần tử trong danh sách thành chuỗi
s.find(sub)	Tìm vị trí đầu tiên của chuỗi con sub
s.count(sub)	Đếm số lần xuất hiện của chuỗi con sub

'''